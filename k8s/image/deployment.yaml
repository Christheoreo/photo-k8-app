apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-worker
  namespace: photo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: image-worker
  template:
    metadata:
      labels:
        app: image-worker
    spec:
      containers:
      - name: image-worker
        image: ghcr.io/christheoreo/photo-k8-app/image-worker:34172f6cb2e903f5cb293b69e26c00f285562bfa
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: image-secret
              key: database-url
        - name: VALKEY_HOST
          value: "valkey"
        - name: VALKEY_PORT
          value: "6379"